<!DOCTYPE html>
<html lang="pl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Materiały</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-4">
    <h1 class="mb-4">📦 Lista materiałów</h1>

    <table class="table table-striped table-bordered shadow-sm">
      <thead class="table-dark">
        <tr>
          <th>Numer</th>
          <th>Nazwa</th>
          <th>Jednostka</th>
          <th>Aktywny</th>
          <th>Podgląd</th>
        </tr>
      </thead>
      <tbody>
        <% if (materials.length === 0) { %>
          <tr><td colspan="5" class="text-center text-muted">Brak materiałów</td></tr>
        <% } else { %>
          <% materials.forEach(m => { %>
            <tr>
              <td><%= m.number %></td>
              <td><%= m.name %></td>
              <td><%= m.unit %></td>
              <td>
                <% if (m.active) { %>
                  <span class="badge text-bg-success">TAK</span>
                <% } else { %>
                  <span class="badge text-bg-secondary">NIE</span>
                <% } %>
              </td>
              <td>
                <% if (m.image_url) { %>
                  <img src="<%= m.image_url %>" alt="img" width="60" />
                <% } else { %>
                  <span class="text-muted">–</span>
                <% } %>
              </td>
            </tr>
          <% }) %>
        <% } %>
      </tbody>
    </table>

    <a href="/" class="btn btn-outline-secondary">⮜ Wróć</a>
  </div>

</body>
</html>
